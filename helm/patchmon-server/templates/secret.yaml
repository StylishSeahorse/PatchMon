{{- if .Values.secret.create -}}
{{- $secretName := printf "%s-secrets" (include "patchmon.fullname" .) -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ $secretName }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "patchmon.labels" . | nindent 4 }}
  {{- with .Values.secret.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
type: Opaque
data:
  # Always use top-level values for secret creation
  {{- if not .Values.database.auth.existingSecret }}
    {{- if not .Values.database.auth.password }}
      {{- fail "You must set database.auth.password or database.auth.existingSecret" }}
    {{- end }}
    postgres-password: {{ .Values.database.auth.password | b64enc | quote }}
  {{- end }}
  {{- if not .Values.redis.auth.existingSecret }}
    {{- if not .Values.redis.auth.password }}
      {{- fail "You must set redis.auth.password or redis.auth.existingSecret" }}
    {{- end }}
    redis-password: {{ .Values.redis.auth.password | b64enc | quote }}
  {{- end }}
  {{- if not .Values.server.existingSecret }}
    {{- if not .Values.server.jwtSecret }}
      {{- fail "You must set server.jwtSecret or server.existingSecret" }}
    {{- end }}
    jwt-secret: {{ .Values.server.jwtSecret | b64enc | quote }}
    {{- if not .Values.server.aiEncryptionKey }}
      {{- fail "You must set server.aiEncryptionKey or server.existingSecret" }}
    {{- end }}
    ai-encryption-key: {{ .Values.server.aiEncryptionKey | b64enc | quote }}
  {{- end }}
  {{- if and .Values.server.oidc.enabled (not .Values.server.oidc.existingSecret) }}
    {{- if not .Values.server.oidc.clientSecret }}
      {{- fail "You must set server.oidc.clientSecret or server.oidc.existingSecret" }}
    {{- end }}
    oidc-client-secret: {{ .Values.server.oidc.clientSecret | b64enc | quote }}
  {{- end }}
{{- end }}
